{% extends('ensemble01filemakerBundle::BSpageBaseFilemaker.html.twig') %}
{% block h1 %}
	<h1 class="page-header">{{ h1|raw }}</h1>
{% endblock h1 %}
{% block body %}
	<div class="col-lg-12">
		{% if databases is iterable %}
		<h3 class="well well-xs">Liste des bases de données<br /><small>{{ databases|length }} résultat(s)</small></h3>
		<table class="table table-bordered table-hover table-condensed">
			<tr class="active">
				<th>Nom de la base</th>
			</tr>
			{% for database in databases %}
			<tr>
				<td>{{ database|raw }}</td>
			</tr>
			{%- endfor %}
		</table>
		{% else %}
		<h3 class="well well-xs">Liste des bases de données<br /><small>Erreur</small></h3>
		<div class="alert alert-danger" role="alert">{{ databases|raw }}</div>
		{% endif %}
	</div>
{% endblock body %}